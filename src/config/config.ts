import { IConfig } from './config.interface';
import { TOKEN } from './token.hidden';

export const config: IConfig = {
  botChannel: 'botspam',
  botName: 'BleepBloop',

  games: ['wow'],

  token: TOKEN,
};
